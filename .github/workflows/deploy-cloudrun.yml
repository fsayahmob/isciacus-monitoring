# =============================================================================
# GitHub Actions - Deploy to Google Cloud Run
# =============================================================================
# Deploys ISCIACUS Monitoring to Cloud Run using Terraform
# Triggered on push to main branch after successful tests
# =============================================================================

name: Deploy to Cloud Run

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: europe-west1
  TERRAFORM_VERSION: '1.6.0'

jobs:
  # ===========================================================================
  # Job 1: Build and Push Docker Images
  # ===========================================================================
  build-images:
    name: 🐳 Build Docker Images
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.sha }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set image tag
        id: meta
        run: echo "sha=${GITHUB_SHA::8}" >> $GITHUB_OUTPUT

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Setup gcloud CLI
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build and push Backend image
        run: |
          IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/isciacus-monitoring/backend"
          docker build -t ${IMAGE}:${{ steps.meta.outputs.sha }} -t ${IMAGE}:latest ./backend
          docker push ${IMAGE}:${{ steps.meta.outputs.sha }}
          docker push ${IMAGE}:latest

      - name: Build and push Frontend image
        run: |
          IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/isciacus-monitoring/frontend"
          docker build -t ${IMAGE}:${{ steps.meta.outputs.sha }} -t ${IMAGE}:latest --target production ./frontend
          docker push ${IMAGE}:${{ steps.meta.outputs.sha }}
          docker push ${IMAGE}:latest

  # ===========================================================================
  # Job 2: Deploy with Terraform
  # ===========================================================================
  deploy:
    name: 🚀 Deploy to Cloud Run
    runs-on: ubuntu-latest
    needs: [build-images]
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Terraform Init
        working-directory: terraform
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="prefix=isciacus-monitoring"

      - name: Terraform Plan
        working-directory: terraform
        run: |
          terraform plan \
            -var="project_id=${{ env.PROJECT_ID }}" \
            -var="region=${{ env.REGION }}" \
            -var="image_tag=${{ needs.build-images.outputs.image_tag }}" \
            -var="pocketbase_admin_password=${{ secrets.PB_ADMIN_PASSWORD }}" \
            -var="shopify_store_url=${{ secrets.SHOPIFY_STORE_URL }}" \
            -var="shopify_access_token=${{ secrets.SHOPIFY_ACCESS_TOKEN }}" \
            -var="ga4_property_id=${{ secrets.GA4_PROPERTY_ID }}" \
            -var="ga4_measurement_id=${{ secrets.GA4_MEASUREMENT_ID }}" \
            -var="inngest_event_key=${{ secrets.INNGEST_EVENT_KEY }}" \
            -var="inngest_signing_key=${{ secrets.INNGEST_SIGNING_KEY }}" \
            -out=tfplan

      - name: Terraform Apply
        working-directory: terraform
        run: terraform apply -auto-approve tfplan

      - name: Get deployment URLs
        working-directory: terraform
        id: outputs
        run: |
          echo "frontend_url=$(terraform output -raw frontend_url)" >> $GITHUB_OUTPUT
          echo "backend_url=$(terraform output -raw backend_url)" >> $GITHUB_OUTPUT
          echo "pocketbase_url=$(terraform output -raw pocketbase_url)" >> $GITHUB_OUTPUT

      - name: Deployment Summary
        run: |
          echo "## 🚀 Deployment Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### URLs" >> $GITHUB_STEP_SUMMARY
          echo "- **Frontend**: ${{ steps.outputs.outputs.frontend_url }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Backend**: ${{ steps.outputs.outputs.backend_url }}" >> $GITHUB_STEP_SUMMARY
          echo "- **PocketBase**: ${{ steps.outputs.outputs.pocketbase_url }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Image Tag" >> $GITHUB_STEP_SUMMARY
          echo "\`${{ needs.build-images.outputs.image_tag }}\`" >> $GITHUB_STEP_SUMMARY

  # ===========================================================================
  # Job 3: Initialize PocketBase Collections
  # ===========================================================================
  init-pocketbase:
    name: 🗃️ Initialize PocketBase
    runs-on: ubuntu-latest
    needs: [deploy]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Wait for PocketBase to be ready
        run: |
          echo "Waiting for PocketBase to be ready..."
          for i in {1..30}; do
            if curl -sf "${{ needs.deploy.outputs.pocketbase_url }}/api/health" > /dev/null 2>&1; then
              echo "✅ PocketBase is ready!"
              exit 0
            fi
            echo "Attempt $i/30..."
            sleep 5
          done
          echo "❌ PocketBase did not become ready in time"
          exit 1

      - name: Initialize collections
        working-directory: backend
        env:
          POCKETBASE_URL: ${{ needs.deploy.outputs.pocketbase_url }}
          PB_ADMIN_EMAIL: admin@isciacus.com
          PB_ADMIN_PASSWORD: ${{ secrets.PB_ADMIN_PASSWORD }}
        run: python scripts/init_pocketbase.py

  # ===========================================================================
  # Job 4: Smoke Tests
  # ===========================================================================
  smoke-tests:
    name: 🧪 Smoke Tests
    runs-on: ubuntu-latest
    needs: [deploy, init-pocketbase]

    steps:
      - name: Test Frontend
        run: |
          echo "Testing frontend..."
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${{ needs.deploy.outputs.frontend_url }}")
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Frontend is responding (HTTP $HTTP_CODE)"
          else
            echo "❌ Frontend returned HTTP $HTTP_CODE"
            exit 1
          fi

      - name: Test Backend Health
        run: |
          echo "Testing backend health endpoint..."
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${{ needs.deploy.outputs.backend_url }}/health")
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Backend health check passed (HTTP $HTTP_CODE)"
          else
            echo "❌ Backend health check failed (HTTP $HTTP_CODE)"
            exit 1
          fi

      - name: Test PocketBase
        run: |
          echo "Testing PocketBase..."
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "${{ needs.deploy.outputs.pocketbase_url }}/api/health")
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ PocketBase is healthy (HTTP $HTTP_CODE)"
          else
            echo "❌ PocketBase health check failed (HTTP $HTTP_CODE)"
            exit 1
          fi

      - name: Summary
        run: |
          echo "## 🧪 Smoke Tests Passed!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All services are responding correctly." >> $GITHUB_STEP_SUMMARY
